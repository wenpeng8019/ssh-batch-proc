# =============================================================================
# 任务配置文件示例
# =============================================================================
# 将此文件复制到任务子目录中，命名为 .eval_config
# 例如: task01/group1/dataset01/.eval_config
#
# 配置优先级: 环境变量 > 配置文件 > 脚本默认值
# =============================================================================

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 远程目录配置 (相对于远程工作目录)
# └─────────────────────────────────────────────────────────────────────────┘

# 远程工作目录 
REMOTE_WORK_DIR=

# 远程输入目录
REMOTE_INPUT_DIR=test_data

# 远程输出目录
REMOTE_OUTPUT_DIR=out

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 模型文件配置 
# │ 远程模型文件路径（相对于远程工作目录，支持最多4个模型）
# │ 留空表示不使用该模型位置 
# └─────────────────────────────────────────────────────────────────────────┘

REMOTE_MODEL_FILE0=model/model.xyz
# REMOTE_MODEL_FILE1=
# REMOTE_MODEL_FILE2=
# REMOTE_MODEL_FILE3=
# REMOTE_MODEL_FILE4=

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 本地目录配置 
# └─────────────────────────────────────────────────────────────────────────┘

# 本地模型目录（存放待上传的模型文件）
# + 留空则默认使用数据集相对路径中的第一级子目录
#   例如: eval.sh task01/group1/dataset01
#   则本地模型目录位于 ./task01
# MODEL_DIR=

# 本地待评测数据集子目录名（相对于评测集子目录）
IN_SUBDIR=yuv

# 评测结果输出子目录名（相对于评测集子目录）
OUT_SUBDIR=output

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 输入文件配置
# └─────────────────────────────────────────────────────────────────────────┘

# 文件后缀列表（逗号分隔）
FILE_SUFFIX_LIST=_y.bin,_uv.bin

# 输入批（tensor）文件数量（默认自动根据后缀数量计算）
# INPUT_NUM=2

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 后处理插件 (必需)
# │ 可选值: resize0, crop1, crop2
# └─────────────────────────────────────────────────────────────────────────┘

POST_PROCESS_PLUGIN=s1_resize

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 批处理配置   
# └─────────────────────────────────────────────────────────────────────────┘

# 每批最大任务数量
MAX_BATCH_SIZE=200
